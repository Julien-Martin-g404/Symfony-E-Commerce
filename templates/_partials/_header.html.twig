<header>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid me-3">
            <div class=""></div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse " id="navbarSupportedContent">
                <ul class="navbar-nav w-100 gap-3 mb-2 mt-3 mb-lg-0 d-flex flex-row category-items justify-content-end">
                    
                    {% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}
                  
                    <li class="nav-item text-end">
                        <a class="nav-link" href="{{ path('admin') }}">Administration</a>
                    </li>
                    {% endif %}
                    <li class="nav-item text-end ">
                        <a class="nav-link" href="{{ path('profile_index') }}">Mon compte</a>
                    </li>
                    <li class="nav-item text-end ">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                    <li class="nav-item panier text-end">
                        <a href="{{ path('cart_index') }}">
                            <img src="{{ asset('assets/uploads/cart4.svg') }}" alt="Panier">
                        </a>
                    </li>
      
                    {% else %}
       
                    <li class="nav-item text-end">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                    <li class="nav-item text-end">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
         
				    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

</header>